{
  "name": "dut-box-web",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen ../../../target/wasm32-unknown-unknown/release/dut_box_web.wasm --out-dir ./src/wasm --target web --no-typescript"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vite-plugin-static-copy": "^3.2.0",
    "vite-plugin-wasm": "^3.3.0"
  }
}
